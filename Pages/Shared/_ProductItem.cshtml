@using QLBH.Models
@model Product

<div class="card my-4 product-card" style="width: 20rem;">
    <a class="card-header text-info" asp-page="ProductPage" asp-route-id="@Model.Id">@Model.Name</a>
    <div class="card-body">
        @if (Model.Images != null && Model.Images.Any())
        {
            <a asp-page="ProductPage" asp-route-id="@Model.Id">
                <img src="@Model.Images.FirstOrDefault()" class="card-img-top product-image" alt="Ảnh sản phẩm">
            </a>
        }
        else
        {
            <p class="text-muted">Không có ảnh</p>
        }
        <p class="card-text">@Model.Description</p>
        <p class="card-text">@Model.Price</p>
        <a href="#" class="btn btn-primary">Đặt hàng</a>
    </div>
</div>

<style>
    .card-header {
        font-size: 1.8rem;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        display: block;
        max-width: 100%;
    }

    .product-image {
        width: 100%;
        height: 250px;
        object-fit: cover;
        border-radius: 8px;
    }

    a {
        font-size: 1.8rem;
    }
</style>