@using QLBH.Models
@model List<Product>
<div class="row">
    @foreach (var product in Model)
    {
        <div class="col-md-4 ">
            @await Html.PartialAsync("_ProductItem", product)
        </div>
    }
</div>